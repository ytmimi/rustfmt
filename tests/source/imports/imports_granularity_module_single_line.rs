// rustfmt-imports_granularity: ModuleSingleLine
use crate::ast::{
    AttrArgs, AttrArgsEq, AttrId, AttrItem, AttrKind, AttrStyle, Attribute, DelimArgs, Lit,
    LitKind, MacDelimiter, MetaItem, MetaItemKind, NestedMetaItem, Path, PathSegment, self,
};
use std::{cmp::Add, path::Path};
